# 一键重装 + 桌面智能还原工具 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 做一个在 Windows 11 里运行的图形工具，重装系统前把桌面备份好，重装后挑着还原，并顺手做系统优化。

**Architecture:** 逻辑与界面彻底分离。核心逻辑写成 5 个 PowerShell 模块（.psm1），纯函数、可被 Pester 自动测试；WPF 界面（.ps1）只负责调用这些模块。重装这种不可逆操作放在带"备份守卫"的执行器里。第一版只做"重置此电脑"档，镜像全新装放二期。

**Tech Stack:** Windows PowerShell 5.1 + WPF（PresentationFramework）+ Pester 5（测试）+ JSON（备份清单格式）。

设计方案原文：`E:\InstallationTools\docs\设计方案-预览.html`

---

## File Structure

```
E:\InstallationTools\
├── src\
│   ├── Modules\
│   │   ├── DesktopScanner.psm1    # 桌面扫描器：扫桌面、解析快捷方式、判断本体盘符
│   │   ├── BackupManager.psm1     # 备份器：写 JSON 清单 + 拷贝文件到安全位置
│   │   ├── RestoreManager.psm1    # 还原器：读清单、还原快捷方式/文件、校验失效
│   │   ├── SystemOptimizer.psm1   # 系统优化器：若干安全可逆的优化项
│   │   └── ReinstallManager.psm1  # 重装执行器：重置档 + 备份守卫
│   ├── UI\
│   │   └── MainWindow.ps1         # WPF 主界面（三大按钮 + 勾选列表）
│   └── Start.ps1                  # 入口：加载模块 + 弹界面
├── tests\
│   ├── DesktopScanner.Tests.ps1
│   ├── BackupManager.Tests.ps1
│   ├── RestoreManager.Tests.ps1
│   ├── SystemOptimizer.Tests.ps1
│   └── ReinstallManager.Tests.ps1
├── backup\                        # 运行时备份输出（清单 JSON + 文件副本）
└── docs\                          # 设计方案、对话记录、本计划
```

**职责边界：** 每个模块只导出几个动词函数（`Get-`/`Invoke-`/`Test-`），界面层不写任何业务逻辑，只调函数 + 显示结果。这样逻辑能自动测，界面能手动验。

---

## Task 0: 项目骨架与测试环境

**Files:**
- Create: `E:\InstallationTools\src\Modules\DesktopScanner.psm1`（先放空函数占位骨架）
- Create: `E:\InstallationTools\tests\DesktopScanner.Tests.ps1`

- [ ] **Step 1: 升级 Pester 到 5.x**

Run:
```powershell
Install-Module Pester -Force -SkipPublisherCheck -Scope CurrentUser -MinimumVersion 5.0.0
Import-Module Pester -MinimumVersion 5.0.0 -Force
Get-Module Pester | Select-Object Name,Version
```
Expected: 显示 Pester 5.x（如机器无外网，记录此限制并改用自带 3.4.0 语法 `Should Be`，但优先用 5.x）。

- [ ] **Step 2: 建空模块骨架**

写入 `src\Modules\DesktopScanner.psm1`：
```powershell
function Get-DesktopItems {
    [CmdletBinding()]
    param([string[]]$Path)
    throw 'not implemented'
}
Export-ModuleMember -Function Get-DesktopItems
```

- [ ] **Step 3: 写一个冒烟测试确认 Pester 跑得通**

写入 `tests\DesktopScanner.Tests.ps1`：
```powershell
BeforeAll {
    Import-Module "$PSScriptRoot\..\src\Modules\DesktopScanner.psm1" -Force
}
Describe 'DesktopScanner 模块加载' {
    It '导出了 Get-DesktopItems' {
        Get-Command Get-DesktopItems -ErrorAction SilentlyContinue | Should -Not -BeNullOrEmpty
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `Invoke-Pester E:\InstallationTools\tests\DesktopScanner.Tests.ps1`
Expected: 1 passed。

- [ ] **Step 5: Commit**

```powershell
cd E:\InstallationTools
git init 2>$null
git add .
git commit -m "chore: 项目骨架与 Pester 测试环境"
```

---

## Task 1: 桌面扫描器（DesktopScanner）

**职责：** 扫描桌面所有项，对每个 `.lnk` 解析其目标路径，判断本体在哪个盘、能否还原；普通文件标为 file。

**Files:**
- Modify: `E:\InstallationTools\src\Modules\DesktopScanner.psm1`
- Test: `E:\InstallationTools\tests\DesktopScanner.Tests.ps1`

- [ ] **Step 1: 写失败测试 — 解析快捷方式目标盘符**

追加到 `tests\DesktopScanner.Tests.ps1`：
```powershell
Describe 'Get-DesktopItems 扫描结果' {
    BeforeAll {
        $script:tmp = Join-Path $env:TEMP ("desk_" + [guid]::NewGuid())
        New-Item -ItemType Directory -Force $script:tmp | Out-Null
        # 造一个普通文件
        Set-Content -Path (Join-Path $script:tmp 'note.txt') -Value 'hello' -Encoding UTF8
        # 造一个指向 C 盘的快捷方式
        $ws = New-Object -ComObject WScript.Shell
        $lnkC = $ws.CreateShortcut((Join-Path $script:tmp 'appC.lnk'))
        $lnkC.TargetPath = 'C:\Program Files\AppC\appC.exe'; $lnkC.Save()
        # 造一个指向 D 盘的快捷方式
        $lnkD = $ws.CreateShortcut((Join-Path $script:tmp 'appD.lnk'))
        $lnkD.TargetPath = 'D:\Tools\appD\appD.exe'; $lnkD.Save()
    }
    AfterAll { Remove-Item $script:tmp -Recurse -Force -ErrorAction SilentlyContinue }

    It '识别普通文件为 file 类型' {
        $items = Get-DesktopItems -Path $script:tmp
        ($items | Where-Object { $_.Name -eq 'note.txt' }).Type | Should -Be 'file'
    }
    It '识别 .lnk 为 shortcut 并解析目标盘符' {
        $items = Get-DesktopItems -Path $script:tmp
        ($items | Where-Object { $_.Name -eq 'appD' }).TargetDrive | Should -Be 'D'
    }
    It 'C 盘本体的快捷方式标为 needreinstall' {
        $items = Get-DesktopItems -Path $script:tmp
        ($items | Where-Object { $_.Name -eq 'appC' }).Status | Should -Be 'needreinstall'
    }
    It 'D 盘本体的快捷方式标为 restorable' {
        $items = Get-DesktopItems -Path $script:tmp
        ($items | Where-Object { $_.Name -eq 'appD' }).Status | Should -Be 'restorable'
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `Invoke-Pester E:\InstallationTools\tests\DesktopScanner.Tests.ps1`
Expected: FAIL（'not implemented'）。

- [ ] **Step 3: 实现 Get-DesktopItems**

替换 `src\Modules\DesktopScanner.psm1` 内容：
```powershell
function Get-DesktopItems {
    <#
      .SYNOPSIS 扫描桌面，返回每个项的结构化信息。
      .PARAMETER Path 要扫描的目录；默认当前用户桌面 + 公共桌面。
    #>
    [CmdletBinding()]
    param([string[]]$Path)

    if (-not $Path) {
        $Path = @(
            [Environment]::GetFolderPath('Desktop'),
            [Environment]::GetFolderPath('CommonDesktopDirectory')
        ) | Where-Object { $_ -and (Test-Path $_) }
    }

    $shell = New-Object -ComObject WScript.Shell
    $results = New-Object System.Collections.Generic.List[object]

    foreach ($dir in $Path) {
        Get-ChildItem -LiteralPath $dir -Force -ErrorAction SilentlyContinue | ForEach-Object {
            $entry = $_
            if ($entry.Extension -eq '.lnk') {
                $sc = $shell.CreateShortcut($entry.FullName)
                $target = $sc.TargetPath
                $drive = ''
                if ($target) {
                    try { $drive = ([System.IO.Path]::GetPathRoot($target)).TrimEnd('\',':') } catch { $drive = '' }
                }
                $status = 'unknown'
                if ($drive) {
                    if ($drive -ieq 'C') { $status = 'needreinstall' }
                    else { $status = 'restorable' }
                }
                $results.Add([pscustomobject]@{
                    Name        = [System.IO.Path]::GetFileNameWithoutExtension($entry.Name)
                    Type        = 'shortcut'
                    SourcePath  = $entry.FullName
                    Target      = $target
                    TargetDrive = $drive
                    Status      = $status
                    SizeBytes   = $entry.Length
                })
            }
            else {
                $results.Add([pscustomobject]@{
                    Name        = $entry.Name
                    Type        = 'file'
                    SourcePath  = $entry.FullName
                    Target      = ''
                    TargetDrive = ''
                    Status      = 'restorable'
                    SizeBytes   = if ($entry.PSIsContainer) { 0 } else { $entry.Length }
                })
            }
        }
    }
    return $results
}
Export-ModuleMember -Function Get-DesktopItems
```

- [ ] **Step 4: 跑测试确认通过**

Run: `Invoke-Pester E:\InstallationTools\tests\DesktopScanner.Tests.ps1`
Expected: 全部 passed。

- [ ] **Step 5: Commit**

```powershell
cd E:\InstallationTools; git add .; git commit -m "feat: 桌面扫描器，识别快捷方式目标盘符与可还原性"
```

---

## Task 2: 备份器（BackupManager）

**职责：** 接收扫描结果 + 用户勾选，把要备份的快捷方式/文件拷到安全目录（非 C 盘），并写一份 JSON 清单。

**Files:**
- Create: `E:\InstallationTools\src\Modules\BackupManager.psm1`
- Test: `E:\InstallationTools\tests\BackupManager.Tests.ps1`

- [ ] **Step 1: 写失败测试**

写入 `tests\BackupManager.Tests.ps1`：
```powershell
BeforeAll {
    Import-Module "$PSScriptRoot\..\src\Modules\BackupManager.psm1" -Force
}
Describe 'Invoke-DesktopBackup' {
    BeforeAll {
        $script:src = Join-Path $env:TEMP ("bsrc_" + [guid]::NewGuid())
        $script:dst = Join-Path $env:TEMP ("bdst_" + [guid]::NewGuid())
        New-Item -ItemType Directory -Force $script:src | Out-Null
        Set-Content (Join-Path $script:src 'a.txt') 'AAA' -Encoding UTF8
        $script:items = @(
            [pscustomobject]@{ Name='a.txt'; Type='file'; SourcePath=(Join-Path $script:src 'a.txt'); Target=''; TargetDrive=''; Status='restorable'; SizeBytes=3; Selected=$true }
            [pscustomobject]@{ Name='skip'; Type='file'; SourcePath=(Join-Path $script:src 'a.txt'); Target=''; TargetDrive=''; Status='restorable'; SizeBytes=3; Selected=$false }
        )
    }
    AfterAll {
        Remove-Item $script:src -Recurse -Force -ErrorAction SilentlyContinue
        Remove-Item $script:dst -Recurse -Force -ErrorAction SilentlyContinue
    }
    It '生成清单 JSON 文件' {
        Invoke-DesktopBackup -Items $script:items -Destination $script:dst
        Test-Path (Join-Path $script:dst 'manifest.json') | Should -Be $true
    }
    It '只拷贝 Selected=$true 的项' {
        Test-Path (Join-Path $script:dst 'files\a.txt') | Should -Be $true
    }
    It '清单里记录了 1 个被选中的项' {
        $m = Get-Content (Join-Path $script:dst 'manifest.json') -Raw | ConvertFrom-Json
        ($m.Items | Where-Object { $_.Selected }).Count | Should -Be 1
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `Invoke-Pester E:\InstallationTools\tests\BackupManager.Tests.ps1`
Expected: FAIL（模块/函数不存在）。

- [ ] **Step 3: 实现 Invoke-DesktopBackup**

写入 `src\Modules\BackupManager.psm1`：
```powershell
function Invoke-DesktopBackup {
    <#
      .SYNOPSIS 把选中的桌面项备份到安全目录，并写 manifest.json。
      .PARAMETER Items 扫描结果（每项需带 Selected 布尔字段）。
      .PARAMETER Destination 备份输出目录（应在非 C 盘）。
    #>
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][object[]]$Items,
        [Parameter(Mandatory)][string]$Destination
    )

    $filesDir = Join-Path $Destination 'files'
    New-Item -ItemType Directory -Force $Destination | Out-Null
    New-Item -ItemType Directory -Force $filesDir | Out-Null

    foreach ($it in ($Items | Where-Object { $_.Selected })) {
        if ($it.SourcePath -and (Test-Path -LiteralPath $it.SourcePath)) {
            $leaf = Split-Path $it.SourcePath -Leaf
            Copy-Item -LiteralPath $it.SourcePath -Destination (Join-Path $filesDir $leaf) -Recurse -Force -ErrorAction SilentlyContinue
        }
    }

    $manifest = [pscustomobject]@{
        CreatedAt = (Get-Date).ToString('s')
        Source    = 'desktop'
        Items     = $Items
    }
    $manifest | ConvertTo-Json -Depth 6 | Set-Content -Path (Join-Path $Destination 'manifest.json') -Encoding UTF8
    return (Join-Path $Destination 'manifest.json')
}
Export-ModuleMember -Function Invoke-DesktopBackup
```

- [ ] **Step 4: 跑测试确认通过**

Run: `Invoke-Pester E:\InstallationTools\tests\BackupManager.Tests.ps1`
Expected: 全部 passed。

- [ ] **Step 5: Commit**

```powershell
cd E:\InstallationTools; git add .; git commit -m "feat: 备份器，拷贝选中项并生成 JSON 清单"
```

---

## Task 3: 还原器（RestoreManager）

**职责：** 读 manifest.json，按用户勾选还原快捷方式/文件到桌面；还原时再校验快捷方式目标是否真存在，不在的标为失效。

**Files:**
- Create: `E:\InstallationTools\src\Modules\RestoreManager.psm1`
- Test: `E:\InstallationTools\tests\RestoreManager.Tests.ps1`

- [ ] **Step 1: 写失败测试**

写入 `tests\RestoreManager.Tests.ps1`：
```powershell
BeforeAll {
    Import-Module "$PSScriptRoot\..\src\Modules\BackupManager.psm1" -Force
    Import-Module "$PSScriptRoot\..\src\Modules\RestoreManager.psm1" -Force
}
Describe 'Invoke-DesktopRestore' {
    BeforeAll {
        $script:src = Join-Path $env:TEMP ("rsrc_" + [guid]::NewGuid())
        $script:dst = Join-Path $env:TEMP ("rdst_" + [guid]::NewGuid())
        $script:desk = Join-Path $env:TEMP ("rdesk_" + [guid]::NewGuid())
        New-Item -ItemType Directory -Force $script:src,$script:desk | Out-Null
        Set-Content (Join-Path $script:src 'a.txt') 'AAA' -Encoding UTF8
        $items = @(
            [pscustomobject]@{ Name='a.txt'; Type='file'; SourcePath=(Join-Path $script:src 'a.txt'); Target=''; TargetDrive=''; Status='restorable'; SizeBytes=3; Selected=$true }
        )
        Invoke-DesktopBackup -Items $items -Destination $script:dst
    }
    AfterAll {
        Remove-Item $script:src,$script:dst,$script:desk -Recurse -Force -ErrorAction SilentlyContinue
    }
    It '把文件还原到目标桌面目录' {
        Invoke-DesktopRestore -Manifest (Join-Path $script:dst 'manifest.json') -DesktopPath $script:desk
        Test-Path (Join-Path $script:desk 'a.txt') | Should -Be $true
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `Invoke-Pester E:\InstallationTools\tests\RestoreManager.Tests.ps1`
Expected: FAIL。

- [ ] **Step 3: 实现 Invoke-DesktopRestore**

写入 `src\Modules\RestoreManager.psm1`：
```powershell
function Invoke-DesktopRestore {
    <#
      .SYNOPSIS 按清单把备份的文件/快捷方式还原到桌面。
      .PARAMETER Manifest manifest.json 路径。
      .PARAMETER DesktopPath 还原目标（默认当前用户桌面）。
      .PARAMETER OnlyNames 仅还原这些 Name（不传则还原清单里 Selected=$true 的全部）。
    #>
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][string]$Manifest,
        [string]$DesktopPath = [Environment]::GetFolderPath('Desktop'),
        [string[]]$OnlyNames
    )
    if (-not (Test-Path -LiteralPath $Manifest)) { throw "找不到清单：$Manifest" }
    $root = Split-Path $Manifest -Parent
    $filesDir = Join-Path $root 'files'
    $data = Get-Content -LiteralPath $Manifest -Raw | ConvertFrom-Json

    $report = New-Object System.Collections.Generic.List[object]
    foreach ($it in $data.Items) {
        if (-not $it.Selected) { continue }
        if ($OnlyNames -and ($it.Name -notin $OnlyNames)) { continue }

        $leaf = Split-Path $it.SourcePath -Leaf
        $backupFile = Join-Path $filesDir $leaf
        $restoreOk = $false; $note = ''

        # 快捷方式：还原前校验目标是否还在
        if ($it.Type -eq 'shortcut' -and $it.Target) {
            if (-not (Test-Path -LiteralPath $it.Target)) {
                $note = '目标本体不存在，可能需重装'
            }
        }
        if (Test-Path -LiteralPath $backupFile) {
            Copy-Item -LiteralPath $backupFile -Destination (Join-Path $DesktopPath $leaf) -Recurse -Force -ErrorAction SilentlyContinue
            $restoreOk = $true
        }
        $report.Add([pscustomobject]@{ Name=$it.Name; Restored=$restoreOk; Note=$note })
    }
    return $report
}
Export-ModuleMember -Function Invoke-DesktopRestore
```

- [ ] **Step 4: 跑测试确认通过**

Run: `Invoke-Pester E:\InstallationTools\tests\RestoreManager.Tests.ps1`
Expected: passed。

- [ ] **Step 5: Commit**

```powershell
cd E:\InstallationTools; git add .; git commit -m "feat: 还原器，按清单还原并校验快捷方式目标"
```

---

## Task 4: 系统优化器（SystemOptimizer）

**职责：** 提供若干**安全、可逆**的优化项，每项独立可选。第一版只做两项无风险的：显示文件扩展名、显示隐藏文件由用户决定。所有写注册表的操作支持 `-WhatIf`（便于测试不真改系统）。

**Files:**
- Create: `E:\InstallationTools\src\Modules\SystemOptimizer.psm1`
- Test: `E:\InstallationTools\tests\SystemOptimizer.Tests.ps1`

- [ ] **Step 1: 写失败测试（用 -WhatIf，不真改系统）**

写入 `tests\SystemOptimizer.Tests.ps1`：
```powershell
BeforeAll {
    Import-Module "$PSScriptRoot\..\src\Modules\SystemOptimizer.psm1" -Force
}
Describe 'Get-OptimizationItems' {
    It '返回一组优化项，每项有 Id/Name/Description' {
        $items = Get-OptimizationItems
        $items.Count | Should -BeGreaterThan 0
        $items[0].PSObject.Properties.Name | Should -Contain 'Id'
        $items[0].PSObject.Properties.Name | Should -Contain 'Name'
    }
}
Describe 'Invoke-Optimization -WhatIf' {
    It 'WhatIf 模式不抛错且不真正写入' {
        { Invoke-Optimization -Id 'show-file-ext' -WhatIf } | Should -Not -Throw
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `Invoke-Pester E:\InstallationTools\tests\SystemOptimizer.Tests.ps1`
Expected: FAIL。

- [ ] **Step 3: 实现优化器**

写入 `src\Modules\SystemOptimizer.psm1`：
```powershell
function Get-OptimizationItems {
    <# .SYNOPSIS 列出可选的优化项。#>
    [CmdletBinding()] param()
    return @(
        [pscustomobject]@{ Id='show-file-ext'; Name='显示文件扩展名'; Description='资源管理器里显示 .txt/.exe 等后缀，避免被伪装' }
        [pscustomobject]@{ Id='show-hidden';   Name='显示隐藏文件';   Description='显示隐藏的文件和文件夹' }
    )
}

function Invoke-Optimization {
    <# .SYNOPSIS 执行某个优化项（支持 -WhatIf）。 #>
    [CmdletBinding(SupportsShouldProcess=$true)]
    param([Parameter(Mandatory)][string]$Id)

    $advanced = 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced'
    switch ($Id) {
        'show-file-ext' {
            if ($PSCmdlet.ShouldProcess('注册表 HideFileExt', '设为 0（显示后缀）')) {
                Set-ItemProperty -Path $advanced -Name 'HideFileExt' -Value 0 -Type DWord
            }
        }
        'show-hidden' {
            if ($PSCmdlet.ShouldProcess('注册表 Hidden', '设为 1（显示隐藏文件）')) {
                Set-ItemProperty -Path $advanced -Name 'Hidden' -Value 1 -Type DWord
            }
        }
        default { throw "未知优化项：$Id" }
    }
}
Export-ModuleMember -Function Get-OptimizationItems, Invoke-Optimization
```

- [ ] **Step 4: 跑测试确认通过**

Run: `Invoke-Pester E:\InstallationTools\tests\SystemOptimizer.Tests.ps1`
Expected: passed。

- [ ] **Step 5: Commit**

```powershell
cd E:\InstallationTools; git add .; git commit -m "feat: 系统优化器，可选优化项 + WhatIf 安全模式"
```

---

## Task 5: 重装执行器（ReinstallManager）— 重置档 + 备份守卫

**职责：** 第一版只封装"重置此电脑"。**关键是守卫逻辑**：重装前必须确认备份清单已存在，否则拒绝执行。真正触发重装的命令默认不执行（`-Confirm` 守卫 + 显式 `-Execute` 开关），自动测试只测守卫逻辑，绝不真重装。

**Files:**
- Create: `E:\InstallationTools\src\Modules\ReinstallManager.psm1`
- Test: `E:\InstallationTools\tests\ReinstallManager.Tests.ps1`

- [ ] **Step 1: 写失败测试（只测守卫，不真重装）**

写入 `tests\ReinstallManager.Tests.ps1`：
```powershell
BeforeAll {
    Import-Module "$PSScriptRoot\..\src\Modules\ReinstallManager.psm1" -Force
}
Describe 'Test-BackupReady' {
    It '清单不存在时返回 $false' {
        Test-BackupReady -ManifestPath (Join-Path $env:TEMP 'no_such_manifest.json') | Should -Be $false
    }
    It '清单存在时返回 $true' {
        $p = Join-Path $env:TEMP ("mok_" + [guid]::NewGuid() + '.json')
        '{}' | Set-Content $p -Encoding UTF8
        Test-BackupReady -ManifestPath $p | Should -Be $true
        Remove-Item $p -Force
    }
}
Describe 'Invoke-SystemReset 守卫' {
    It '没备份时拒绝执行并抛错' {
        { Invoke-SystemReset -ManifestPath (Join-Path $env:TEMP 'no_such.json') -Execute } |
            Should -Throw -ExpectedMessage '*未检测到备份*'
    }
    It '不带 -Execute 时为演练，不抛错也不重装' {
        $p = Join-Path $env:TEMP ("mok2_" + [guid]::NewGuid() + '.json')
        '{}' | Set-Content $p -Encoding UTF8
        { Invoke-SystemReset -ManifestPath $p } | Should -Not -Throw
        Remove-Item $p -Force
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `Invoke-Pester E:\InstallationTools\tests\ReinstallManager.Tests.ps1`
Expected: FAIL。

- [ ] **Step 3: 实现重装执行器**

写入 `src\Modules\ReinstallManager.psm1`：
```powershell
function Test-BackupReady {
    <# .SYNOPSIS 检查备份清单是否已生成。#>
    [CmdletBinding()] param([Parameter(Mandatory)][string]$ManifestPath)
    return (Test-Path -LiteralPath $ManifestPath)
}

function Invoke-SystemReset {
    <#
      .SYNOPSIS 触发 Windows“重置此电脑”。带备份守卫。
      .PARAMETER ManifestPath 备份清单路径，必须已存在才允许重装。
      .PARAMETER KeepFiles 是否保留个人文件（$true=保留，$false=全部删除）。
      .PARAMETER Execute 真正执行重装的开关；不带则只演练打印，不动系统。
    #>
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)][string]$ManifestPath,
        [bool]$KeepFiles = $true,
        [switch]$Execute
    )
    if (-not (Test-BackupReady -ManifestPath $ManifestPath)) {
        throw '未检测到备份清单，已阻止重装。请先完成“备份桌面”。'
    }
    if (-not $Execute) {
        Write-Host '[演练] 备份已就绪。加 -Execute 才会真正重置此电脑。' -ForegroundColor Yellow
        return $false
    }
    # 真实重装：调用系统重置向导（用户需在系统向导里再确认一次）
    $arg = if ($KeepFiles) { '-AllowDataLoss disabled' } else { '-CleanPC' }
    Write-Host '正在唤起“重置此电脑”向导…' -ForegroundColor Cyan
    Start-Process -FilePath 'systemreset.exe' -ArgumentList $arg -ErrorAction Stop
    return $true
}
Export-ModuleMember -Function Test-BackupReady, Invoke-SystemReset
```

> 说明：`systemreset.exe` 是 Windows 自带的"重置此电脑"程序，会弹官方向导让用户再确认一次，安全。`-Execute` 是双保险，自动测试永远不传它。

- [ ] **Step 4: 跑测试确认通过**

Run: `Invoke-Pester E:\InstallationTools\tests\ReinstallManager.Tests.ps1`
Expected: passed。

- [ ] **Step 5: Commit**

```powershell
cd E:\InstallationTools; git add .; git commit -m "feat: 重装执行器，重置档 + 备份守卫(默认演练)"
```

---

## Task 6: WPF 主界面（MainWindow）

**职责：** 三大按钮（备份→重装→还原优化）+ 一个带复选框的列表。只调用前面模块的函数，不写业务逻辑。此任务靠**手动验证**（GUI 难自动测）。

**Files:**
- Create: `E:\InstallationTools\src\UI\MainWindow.ps1`

- [ ] **Step 1: 写界面脚本**

写入 `src\UI\MainWindow.ps1`：
```powershell
Add-Type -AssemblyName PresentationFramework

# 加载模块
$mod = Join-Path $PSScriptRoot '..\Modules'
'DesktopScanner','BackupManager','RestoreManager','SystemOptimizer','ReinstallManager' |
    ForEach-Object { Import-Module (Join-Path $mod "$_.psm1") -Force }

$backupRoot = 'E:\InstallationTools\backup'
$manifest   = Join-Path $backupRoot 'manifest.json'

[xml]$xaml = @'
<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        Title="一键重装 + 桌面智能还原" Height="560" Width="780" WindowStartupLocation="CenterScreen">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
      <Button x:Name="BtnScan"    Content="1 扫描+备份桌面" Width="160" Height="40" Margin="0,0,8,0"/>
      <Button x:Name="BtnReset"   Content="2 重装系统(重置)" Width="160" Height="40" Margin="0,0,8,0"/>
      <Button x:Name="BtnRestore" Content="3 还原+优化"      Width="160" Height="40"/>
    </StackPanel>
    <DataGrid x:Name="Grid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False">
      <DataGrid.Columns>
        <DataGridCheckBoxColumn Header="选" Binding="{Binding Selected}" Width="40"/>
        <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="180"/>
        <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="80"/>
        <DataGridTextColumn Header="本体盘" Binding="{Binding TargetDrive}" Width="70"/>
        <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="120"/>
        <DataGridTextColumn Header="目标路径" Binding="{Binding Target}" Width="*"/>
      </DataGrid.Columns>
    </DataGrid>
    <TextBlock x:Name="StatusBar" Grid.Row="2" Margin="0,8,0,0" Foreground="Gray"/>
  </Grid>
</Window>
'@

$reader = New-Object System.Xml.XmlNodeReader $xaml
$win = [Windows.Markup.XamlReader]::Load($reader)
$Grid      = $win.FindName('Grid')
$BtnScan   = $win.FindName('BtnScan')
$BtnReset  = $win.FindName('BtnReset')
$BtnRestore= $win.FindName('BtnRestore')
$StatusBar = $win.FindName('StatusBar')

$BtnScan.Add_Click({
    $items = Get-DesktopItems | ForEach-Object {
        $_ | Add-Member -NotePropertyName Selected -NotePropertyValue ($_.Status -ne 'needreinstall') -PassThru
    }
    $Grid.ItemsSource = @($items)
    $StatusBar.Text = "已扫描 $($items.Count) 项。勾选后再点“扫描+备份”确认备份。"
    $script:scanned = $items
    # 第二次点击执行备份
    if ($script:hasScanned) {
        Invoke-DesktopBackup -Items @($Grid.ItemsSource) -Destination $backupRoot | Out-Null
        $StatusBar.Text = "已备份到 $backupRoot"
    }
    $script:hasScanned = $true
})

$BtnReset.Add_Click({
    if (-not (Test-BackupReady -ManifestPath $manifest)) {
        [System.Windows.MessageBox]::Show('还没备份！请先点“1 扫描+备份桌面”。','阻止重装','OK','Warning'); return
    }
    $r = [System.Windows.MessageBox]::Show('确认已完成备份，现在重置此电脑？','二次确认','YesNo','Warning')
    if ($r -eq 'Yes') { Invoke-SystemReset -ManifestPath $manifest -KeepFiles $true -Execute }
})

$BtnRestore.Add_Click({
    if (-not (Test-BackupReady -ManifestPath $manifest)) {
        [System.Windows.MessageBox]::Show('找不到备份清单，无法还原。','提示','OK','Warning'); return
    }
    $rep = Invoke-DesktopRestore -Manifest $manifest
    Get-OptimizationItems | ForEach-Object { Invoke-Optimization -Id $_.Id }
    $StatusBar.Text = "已还原 $(@($rep).Count) 项并应用优化。"
})

$win.ShowDialog() | Out-Null
```

- [ ] **Step 2: 手动验证界面能弹出**

Run: `powershell -ExecutionPolicy Bypass -File E:\InstallationTools\src\UI\MainWindow.ps1`
Expected: 弹出窗口，三个按钮 + 空表格。点"1 扫描+备份桌面"应列出当前桌面项。

- [ ] **Step 3: Commit**

```powershell
cd E:\InstallationTools; git add .; git commit -m "feat: WPF 主界面，三大按钮 + 勾选列表"
```

---

## Task 7: 入口脚本与联调

**Files:**
- Create: `E:\InstallationTools\src\Start.ps1`
- Create: `E:\InstallationTools\一键启动.bat`

- [ ] **Step 1: 写入口脚本**

写入 `src\Start.ps1`：
```powershell
# 入口：检查管理员权限（重装/优化需要），然后启动界面
$isAdmin = ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltinRole]::Administrator)
if (-not $isAdmin) {
    Write-Warning '建议以管理员身份运行（重装/部分优化需要）。'
}
& (Join-Path $PSScriptRoot 'UI\MainWindow.ps1')
```

- [ ] **Step 2: 写双击启动的 bat（自动请求管理员）**

写入 `E:\InstallationTools\一键启动.bat`：
```bat
@echo off
powershell -NoProfile -ExecutionPolicy Bypass -Command "Start-Process powershell -Verb RunAs -ArgumentList '-NoProfile -ExecutionPolicy Bypass -File ""E:\InstallationTools\src\Start.ps1""'"
```

- [ ] **Step 3: 跑全部测试做总验证**

Run: `Invoke-Pester E:\InstallationTools\tests`
Expected: 所有测试 passed。

- [ ] **Step 4: 手动联调（不真重装）**

1. 双击 `一键启动.bat` → 同意 UAC。
2. 点"1 扫描+备份桌面" → 看到桌面项列表。
3. 再点一次 → 检查 `E:\InstallationTools\backup\manifest.json` 和 `backup\files\` 已生成。
4. 点"2 重装系统(重置)" → 弹二次确认（**测试时点 No，不真重装**）。
5. 点"3 还原+优化" → 状态栏显示还原结果。

- [ ] **Step 5: Commit**

```powershell
cd E:\InstallationTools; git add .; git commit -m "feat: 入口脚本 + 一键启动 bat，完成第一版联调"
```

---

## Self-Review（计划自查）

**Spec 覆盖检查：**
- ✅ 桌面扫描/区分本体盘符 → Task 1
- ✅ 备份(选中项 + 清单) → Task 2
- ✅ 勾选还原 + 校验失效 → Task 3
- ✅ C 盘软件只标记(needreinstall) → Task 1 状态 + Task 3 note，界面不替装
- ✅ 系统优化(可选项) → Task 4
- ✅ 重装·重置档 + 备份守卫 → Task 5
- ✅ WPF 三按钮界面 → Task 6
- ✅ 入口/启动 → Task 7
- 🕒 镜像全新装 → 二期（设计⑦已说明，本计划不含，符合范围）

**占位符扫描：** 无 TBD/TODO，每步含真实代码与命令。

**类型一致性：** 各模块字段名统一（Name/Type/SourcePath/Target/TargetDrive/Status/SizeBytes/Selected）；函数名贯穿一致（Get-DesktopItems / Invoke-DesktopBackup / Invoke-DesktopRestore / Get-OptimizationItems / Invoke-Optimization / Test-BackupReady / Invoke-SystemReset）。

**已知取舍：**
- WPF 界面与真实重装无法自动测，靠手动验证（已在 Task 6/7 标注）。
- "扫描+备份"按钮用两次点击（先扫描勾选、再点确认备份）——执行时若觉得别扭，可在执行阶段拆成两个按钮。
